#include <bits/stdc++.h>

using namespace std;

int main()
{
int n,d;
    cin>>n;
    int array[n];
    cin>>d;
    for(int i=0;i<n;cin>>array[i],i++);
    d=d%n;
    for(int i=d;i<n;i++)
        cout<<array[i]<<" ";
    
    for(int i=0;i<d;i++)
         cout<<array[i]<<" ";
    return 0;
}
