
void printPatt(int n)
{
     
    int i,j,num=1;
    for(i=0;i<n;i++)
    {
        for(j=0;j<pow(2,i);j++)
        {
            if(num>9)
                num=1;
            
            printf("%d",num);
            num++;
          
        }
    printf("\n");
        
    }
}
